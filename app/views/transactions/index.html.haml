%h1.page-header Listing transactions
.table-responsive
  %table.table.table-striped
    %thead
      %tr
        %th User
        %th Category
        %th Debit
        %th Credit
        %th Amount
        %th{:colspan => "3"}
    %tbody
      - @transactions.each do |transaction|
        %tr
          %td= transaction.user_id
          %td= transaction.category_id
          %td= transaction.debit_id
          %td= transaction.credit_id
          %td= transaction.amount
          %td= link_to 'Show', transaction
          %td= link_to 'Edit', edit_transaction_path(transaction)
          %td= link_to 'Destroy', transaction, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Transaction', new_transaction_path
